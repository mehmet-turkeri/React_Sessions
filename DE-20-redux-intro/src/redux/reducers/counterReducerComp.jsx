const counterReducerComp=(state1={countSonuc:0},action1)=>{
    switch (action1.type) {
        case "RESET":
        return {counterSonuc:0, payload1:payload1}
            
            break;
    
        default:
            break;
    }
}

export default counterReducerComp;